![[Pasted image 20260423150159.png]]
RL公式的问题是，它的基本对象（state, action, reward, policy）是人为定义的抽象，不是自然界给你的。所以你没法"观察"出来，只能从设计意图出发理解它。

**这个式子的设计意图**
拆开看每一个决策为什么必须存在：

$\pi^*$ — 我们在找一个策略，不是一个数，不是一个状态。策略是从状态到动作的映射，这是RL问题的答案形式。

$\arg\max_\pi$ — 策略空间里有很多策略，我们要找最优的那个。"最优"需要一个标准，所以后面必须跟一个关于 $\pi$ 的标量。

$\mathbb{E}[\cdot|\pi]$ — 环境是随机的（同样的action在不同时刻可能到达不同的 $S_{t+1}$），所以单次轨迹的回报是随机变量。取期望是把随机性平均掉，得到策略的稳定评价。  $\mathbb{E}[⋅∣π]$ 表示在**给定策略 π 的条件下**的**条件数学期望（Conditional Expectation）**。

$\sum_{t=0}^{H} \gamma^t R_t$ — 单步reward不够，要累积。但未来的reward要打折（$\gamma < 1$），原因有两个：一是数学上保证无穷horizon时级数收敛，二是直觉上"现在的奖励比未来的奖励更确定"。

$R_t(S_t, A_t, S_{t+1})$ — reward依赖于当前状态、采取的动作、以及转移到的下一状态。写成这个形式是最一般的，很多情况下可以简化成 $R(S_t, A_t)$。

**为什么你"写不出来"**

不是因为你不懂数学，而是因为你还没有内化RL要解决的问题的形状。

物理里你能推 $F=ma$ 是因为你知道"力让物体加速"这件事在先，公式是对这件事的精确化。

RL里对应的前置直觉是：**一个agent在不知道环境模型的情况下，通过试错找到长期累积收益最大的行为方式。** 一旦这句话的每个词都有具体含义——"不知道环境模型"意味着要采样，"长期"意味着要求和，"不确定性"意味着要取期望——公式就是这句话的直接翻译。
所以缺的不是推导能力，而是在看公式之前先能说出那句话。


## "策略退化"--为什么需要交叉熵。

**策略是什么**

策略 $\pi(a)$ 是一个概率分布，定义在所有可能的动作上。比如有三个动作：

$$\pi(a_1) = 0.2, \quad \pi(a_2) = 0.5, \quad \pi(a_3) = 0.3$$

每次决策时按这个分布随机采样动作。
**只最大化期望reward会发生什么**
假设三个动作的reward是：
$$r(a_1) = 1, \quad r(a_2) = 5, \quad r(a_3) = 2$$

期望reward是：

$$\mathbb{E}[r] = 0.2 \times 1 + 0.5 \times 5 + 0.3 \times 2 = 3.3$$

但如果我把所有概率压到 $a_2$：

$$\pi(a_1) = 0, \quad \pi(a_2) = 1, \quad \pi(a_3) = 0$$

期望reward变成：

$$\mathbb{E}[r] = 0 \times 1 + 1 \times 5 + 0 \times 2 = 5$$

这比任何混合策略都大。所以纯粹最大化期望reward，最优解永远是把全部概率堆在reward最大的那个动作上。这就是"退化"——策略不再是分布，变成了一个确定性的点。

**为什么这是问题**

在bandit（单步）问题里，你已经知道每个动作的reward，退化没关系。

但在真实RL里，reward是未知的，你是在学习过程中估计的。估计有误差，如果策略完全greedy，你永远只试reward估计值最高的那个动作，其他动作的真实reward永远不知道。可能 $a_3$ 的真实reward是100，但你的初始估计是2，你就永远错过它了。

这是**exploitation和exploration的矛盾**：greedy策略只利用已有知识，不探索未知。

**熵惩罚怎么解决这个问题**

熵 $\mathcal{H}(\pi) = -\sum_a \pi(a)\log\pi(a)$ 衡量分布的"分散程度"：

- 全部压在一个动作：$\mathcal{H} = 0$（最小）
- 均匀分布在所有动作：$\mathcal{H} = \log N$（最大）

加上 $\beta \mathcal{H}(\pi)$ 之后，目标函数会惩罚过于集中的策略。最优解不再是delta函数，而是一个有温度的softmax——reward高的动作概率更大，但其他动作也保留一定概率。$\beta$ 就是你愿意为探索付出多少reward代价。


**这页的核心问题**

普通RL只最大化期望reward $\mathbb{E}[r(a)]$。但这样的最优策略是退化的——它会把所有概率压在一个动作上（greedy），完全不探索。

Max-entropy RL加了一个惩罚项：策略的熵 $\mathcal{H}(\pi)$。目标变成：

$$\max_{\pi(a)} \underbrace{\mathbb{E}[r(a)]}_{\text{要reward高}} + \beta \underbrace{\mathcal{H}(\pi(a))}_{\text{也要不确定}}$$

$\beta$ 控制你有多在意探索。这是整个推导的出发点。

**第二行：把期望和熵展开成显式求和**

$$\max_{\pi(a)} \sum_a \pi(a)r(a) - \beta \sum_a \pi(a)\log\pi(a)$$

熵的定义是 $\mathcal{H}(\pi) = -\sum_a \pi(a)\log\pi(a)$，所以加熵等于减去 $\sum \pi \log \pi$。符号变化注意一下。

**第三行：解这个优化问题**

对 $\pi(a)$ 求变分（加上归一化约束 $\sum_a \pi(a)=1$，用拉格朗日乘子），解出来的最优策略是：

$$\pi^*(a) = \frac{1}{Z}\exp!\left(\frac{1}{\beta}r(a)\right)$$

这就是Boltzmann分布/softmax。reward高的动作概率指数级更大，$\beta$ 越小越集中，$\beta$ 越大越均匀。$Z$ 是归一化常数。

**下面的大块：把最优策略代回去算最优值 $V$**

这一步是把 $\pi^*$ 代入目标函数，看最优值是多少。结果是：

$$V = \beta \log \sum_a \exp!\left(\frac{1}{\beta}r(a)\right)$$

这正是softmax（log-sum-exp）函数。

推导里有一步跳跃值得注意：第三行到第四行，$\sum_a \frac{1}{Z}\exp(\frac{1}{\beta}r(a))(r(a) - \frac{1}{\beta}r(a))$ 里面 $r(a)$ 和 $\frac{1}{\beta}r(a)$ 的差被整理成0，是因为 $\mathbb{E}_{\pi^*}[r(a) - \beta \cdot \frac{1}{\beta}r(a)] = 0$。

**整页的意义**

max-entropy RL的最优值函数 = softmax of rewards。这不是巧合，这说明softmax本质上是"在reward和探索之间做最优权衡"的解。神经网络最后一层用softmax，背后有这个信息论基础。

哪一步具体看不下去？